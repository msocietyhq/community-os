{
  "name": "community-os",
  "private": true,
  "workspaces": ["apps/*", "packages/*"],
  "catalog": {
    "better-auth": "^1.5.5",
    "better-auth-telegram": "^1.5.0",
    "zod": "^4.3.6"
  },
  "scripts": {
    "dev": "concurrently -n api,web -c blue,cyan --kill-others-on-fail \"bun run --cwd apps/api dev\" \"wait-on tcp:3000 && bun run --cwd apps/web dev\"",
    "build": "bun run --filter '*' build",
    "lint": "biome check .",
    "lint:fix": "biome check --write .",
    "type-check": "bun run --filter '*' type-check",
    "db:generate": "bun run --filter @community-os/api db:generate",
    "db:migrate": "bun run --filter @community-os/api db:migrate",
    "db:seed": "bun run --filter @community-os/api db:seed"
  },
  "devDependencies": {
    "@typescript/native-preview": "^7.0.0-dev.20260313.1",
    "bun-types": "^1.3.10",
    "concurrently": "^9.2.1",
    "wait-on": "^9.0.4"
  },
  "dependencies": {
    "@grammyjs/conversations": "^2.1.1"
  }
}
