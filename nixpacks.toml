# Fallback if the API still builds with Nixpacks. Railway's default builder
# is Railpack (see railpack.json), which ignores this file.
# `"..."` keeps the provider's packages (bun, etc.).
[phases.setup]
nixPkgs = ["...", "openssh"]
aptPkgs = ["openssh-client"]
